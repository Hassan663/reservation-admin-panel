import React from 'react';

const Dashboard = () => {
  return (
    <div
      style={{
        fontSize: '25px',
        fontWeight: '500',
        justifyContent: 'center',
        alignItems: 'center',
        display: 'flex',
        height: '100vh',
      }}
    >
      Dashboard
    </div>
  );
};

export default Dashboard;
